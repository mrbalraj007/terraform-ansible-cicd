<!DOCTYPE html>
<html>
<head><title>{{ inventory_hostname }}</title></head>
<body>
  <h1>Deployed by Ansible via GitHub Actions</h1>
  <p>Host: {{ inventory_hostname }}</p>
  <p>Environment: {{ hostvars[inventory_hostname]['tags']['Environment'] | default('unknown') }}</p>
</body>
</html>
